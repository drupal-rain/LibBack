<?php

// drupal_set_*();
// @api http://drupal7.api/api/drupal/7.x/search/drupal_set

drupal_set_title($title = NULL, $output = CHECK_PLAIN);

drupal_set_message($message = NULL, $type = 'status', $repeat = TRUE);

drupal_set_breadcrumb($breadcrumb = NULL);

drupal_set_page_content($content = NULL);

drupal_attributes(array $attributes = array());
